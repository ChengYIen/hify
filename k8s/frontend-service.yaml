apiVersion: v1
kind: Service
metadata:
  name: hify-frontend
  labels:
    app.kubernetes.io/name: hify-frontend
    app.kubernetes.io/part-of: hify
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: hify-frontend
  ports:
    - name: http
      port: 80
      targetPort: http
      nodePort: 30080
