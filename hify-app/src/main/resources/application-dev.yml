# ======================== 开发环境 ========================
spring:
  datasource:
    mysql:
      jdbc-url: jdbc:mysql://localhost:3306/hify_dev?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai
      username: root
      password: 123456
    postgresql:
      # 本机便携版 PG 运行在 5433（见 C:\Users\24454\pgdata，PG 17 + pgvector）
      jdbc-url: jdbc:postgresql://localhost:5433/hify_dev
      username: hify
      password: hify

  data:
    redis:
      host: localhost
      port: 6379
      password:

server:
  port: 8080

logging:
  level:
    com.hify: debug
    org.apache.ibatis: debug
